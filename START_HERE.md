# 🚀 ابدأ من هنا - وصلني عامل

## خطوات سريعة لتشغيل المشروع

### 1️⃣ تثبيت المتطلبات
```bash
# تأكد من تثبيت Node.js 18+
node --version

# تثبيت المكتبات
pnpm install
```

### 2️⃣ إعداد Supabase
1. اذهب إلى [supabase.com](https://supabase.com) وأنشئ مشروع جديد
2. احفظ **Project URL** و **Anon Key**
3. في Supabase Dashboard > SQL Editor، شغّل الملفات من `scripts/` بالترتيب:
   - 001 → 010 (الملفات الأساسية)
   - 011 (اختياري - للتوافق)

### 3️⃣ إعداد متغيرات البيئة
```bash
# انسخ ملف env.example
cp env.example .env.local

# عدّل .env.local وأضف قيمك من Supabase
```

**الحد الأدنى المطلوب:**
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/dashboard
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 4️⃣ تشغيل المشروع
```bash
pnpm dev
```

افتح المتصفح على: **http://localhost:3000**

## ✅ التحقق من التشغيل

1. **الصفحة الرئيسية** - يجب أن تفتح بدون أخطاء
2. **التسجيل** - اذهب إلى `/auth/sign-up` وجرب إنشاء حساب
3. **لوحة التحكم** - بعد التسجيل، يجب أن تنتقل إلى `/dashboard`

## 📚 المزيد من المعلومات

- **دليل الإعداد الكامل:** [SETUP_GUIDE.md](./SETUP_GUIDE.md)
- **التحقق من الإعداد:** [SETUP_VERIFICATION.md](./SETUP_VERIFICATION.md)
- **قائمة ما ينقص:** [MISSING_ITEMS.md](./MISSING_ITEMS.md)

## ⚠️ ملاحظات مهمة

- **Stripe غير مطلوب** للاختبار الأساسي - يمكنك تجربة باقي الوظائف بدون Stripe
- **الخريطة** - حالياً تعرض placeholder فقط
- **رفع الصور** - يحتاج إعداد Storage buckets في Supabase (اختياري)

## 🐛 مشاكل شائعة

### "Invalid API key"
→ تأكد من نسخ القيم بشكل صحيح من Supabase

### "relation does not exist"
→ تأكد من تشغيل جميع SQL scripts بالترتيب

### الصفحة لا تعمل
→ تأكد من تشغيل `pnpm dev` والتحقق من console للأخطاء

---

**جاهز للبدء؟** ابدأ بالخطوة 1️⃣ أعلاه! 🎉


