# قائمة التحقق من النشر - وصلني عامل

## 🔴 مشاكل حرجة يجب إصلاحها قبل النشر

### 1. متغيرات البيئة (Environment Variables)
**المشكلة:** لا يوجد ملف `.env.example` لتوثيق المتغيرات المطلوبة

**المطلوب:**
- إنشاء ملف `.env.example` يحتوي على:
  ```
  NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
  NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
  STRIPE_SECRET_KEY=your_stripe_secret_key
  NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/dashboard
  ```

### 2. مشكلة في قاعدة البيانات - `worker_profiles` vs `workers`
**المشكلة:** الكود يستخدم `worker_profiles` في أماكن كثيرة بينما الجدول في قاعدة البيانات اسمه `workers`

**الأماكن المتأثرة:**
- `app/dashboard/page.tsx` (السطر 26)
- `app/requests/new/page.tsx` (السطر 43)
- `app/requests/[id]/payment/page.tsx` (السطر 28)
- `app/actions/stripe.ts` (السطر 19)
- `app/workers/[id]/page.tsx`
- `app/workers/page.tsx`
- `app/worker/profile/page.tsx`
- `app/subscriptions/page.tsx`
- `app/chats/[id]/page.tsx`
- `app/chats/page.tsx`
- `app/bids/[requestId]/page.tsx`
- `app/admin/workers/page.tsx`

**الحل:** يجب إما:
- إنشاء view أو alias باسم `worker_profiles` يشير إلى `workers`
- أو تعديل كل الكود لاستخدام `workers` بدلاً من `worker_profiles`

### 3. مشكلة في إنشاء الطلبات - حقل `address` مفقود
**المشكلة:** في `app/requests/new/page.tsx`، الكود يحفظ `location` لكن قاعدة البيانات تتوقع `address`

**الحل:** تعديل الكود لاستخدام `address` بدلاً من `location` أو إضافة `address` للـ form

### 4. عدم وجود API Routes للـ Webhooks
**المشكلة:** لا توجد routes لمعالجة Stripe webhooks

**المطلوب:**
- إنشاء `app/api/webhooks/stripe/route.ts` لمعالجة أحداث Stripe
- تحديث حالة الدفع عند اكتمال الدفع
- تحديث حالة الطلب تلقائياً

### 5. عدم وجود نظام رفع الصور
**المشكلة:** لا يوجد نظام لرفع الصور (portfolio, problem_images, avatar)

**المطلوب:**
- إعداد Supabase Storage أو خدمة أخرى
- إنشاء API routes لرفع الصور
- تحديث الكود لدعم رفع الصور

### 6. الخريطة غير مفعلة
**المشكلة:** `components/map-view.tsx` يعرض placeholder فقط

**المطلوب:**
- إضافة Google Maps API أو Mapbox
- تفعيل الخريطة التفاعلية
- إضافة متغير بيئة لـ API key

---

## 🟡 مشاكل مهمة يجب معالجتها

### 7. معالجة الأخطاء غير كافية
**المشكلة:** العديد من الأخطاء لا يتم معالجتها بشكل صحيح

**المطلوب:**
- إضافة error boundaries
- تحسين رسائل الخطأ للمستخدم
- إضافة logging للأخطاء

### 8. التحقق من الصلاحيات (Authorization)
**المشكلة:** بعض الصفحات لا تتحقق بشكل كافٍ من صلاحيات المستخدم

**المطلوب:**
- إضافة middleware للتحقق من الأدوار
- التأكد من أن العمال لا يمكنهم الوصول لصفحات العملاء والعكس
- إضافة حماية للصفحات الإدارية

### 9. عدم وجود نظام إشعارات حقيقي
**المشكلة:** صفحة الإشعارات موجودة لكن لا يوجد نظام إشعارات فعلي

**المطلوب:**
- إعداد Supabase Realtime أو خدمة إشعارات
- إضافة إشعارات للطلبات الجديدة، الرسائل، التقييمات

### 10. نظام الدفع غير مكتمل
**المشكلة:** 
- لا يوجد webhook handler
- لا يوجد تحديث تلقائي لحالة الدفع
- لا يوجد نظام استرجاع الأموال

**المطلوب:**
- إكمال نظام الدفع
- إضافة webhook handlers
- إضافة صفحة لتتبع المدفوعات

### 11. نظام التقييمات والمراجعات غير موجود
**المشكلة:** الكود يشير إلى التقييمات لكن لا توجد صفحات لإنشائها

**المطلوب:**
- إنشاء صفحة `/requests/[id]/review`
- إضافة نظام التقييمات
- تحديث تقييم العامل تلقائياً

---

## 🟢 تحسينات موصى بها

### 12. SEO و Meta Tags
**المطلوب:**
- إضافة meta tags ديناميكية لكل صفحة
- إضافة Open Graph tags
- إضافة structured data (JSON-LD)

### 13. الأداء
**المطلوب:**
- إضافة loading states أفضل
- تحسين الصور (استخدام next/image بشكل صحيح)
- إضافة caching للبيانات الثابتة

### 14. الأمان
**المطلوب:**
- إضافة rate limiting
- تحسين CORS policies
- إضافة CSRF protection
- فحص SQL injection vulnerabilities

### 15. التوثيق
**المطلوب:**
- تحديث README.md بتعليمات الإعداد
- إضافة توثيق للـ API
- إضافة تعليقات في الكود

### 16. الاختبارات
**المطلوب:**
- إضافة unit tests
- إضافة integration tests
- إضافة E2E tests

### 17. التحليلات
**المطلوب:**
- إضافة Google Analytics أو Vercel Analytics
- تتبع الأحداث المهمة
- إضافة dashboard للتحليلات

### 18. دعم متعدد اللغات
**المطلوب:**
- إضافة i18n للدعم الإنجليزي
- إضافة language switcher

### 19. نظام البحث والفلترة
**المطلوب:**
- تحسين البحث عن العمال
- إضافة فلاتر متقدمة
- إضافة sorting options

### 20. نظام التذكير والمواعيد
**المطلوب:**
- إضافة تذكيرات للطلبات
- إضافة تقويم للمواعيد
- إشعارات قبل الموعد

---

## 📋 قائمة سريعة للإعداد

### قبل النشر مباشرة:

- [ ] إصلاح مشكلة `worker_profiles` vs `workers`
- [ ] إصلاح مشكلة `address` في الطلبات
- [ ] إضافة ملف `.env.example`
- [ ] إعداد متغيرات البيئة في Vercel/Supabase
- [ ] إنشاء API route لـ Stripe webhooks
- [ ] إعداد Supabase Storage للصور
- [ ] تفعيل الخريطة (Google Maps/Mapbox)
- [ ] إضافة error handling أفضل
- [ ] اختبار جميع الصفحات الرئيسية
- [ ] اختبار نظام المصادقة
- [ ] اختبار إنشاء الطلبات
- [ ] اختبار نظام الدفع
- [ ] إضافة نظام التقييمات
- [ ] إعداد نظام الإشعارات
- [ ] إضافة SEO tags
- [ ] تحديث README.md

---

## 🚀 خطوات النشر

1. **إعداد Supabase:**
   - إنشاء مشروع جديد
   - تشغيل جميع SQL scripts بالترتيب
   - إعداد Storage buckets
   - إعداد RLS policies

2. **إعداد Stripe:**
   - إنشاء حساب
   - إعداد webhook endpoint
   - إضافة API keys

3. **إعداد Vercel:**
   - ربط المشروع
   - إضافة environment variables
   - تفعيل deployment

4. **الاختبار:**
   - اختبار جميع الوظائف
   - اختبار على أجهزة مختلفة
   - اختبار الأداء

5. **المراقبة:**
   - إعداد error tracking (Sentry)
   - إعداد analytics
   - مراقبة الأداء

