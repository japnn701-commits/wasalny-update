# إعداد قاعدة البيانات - وصلني عامل

## 📋 خطوات إعداد قاعدة البيانات في Supabase

### الخطوة 1: فتح SQL Editor
1. اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. من القائمة الجانبية، اضغط على **SQL Editor**

### الخطوة 2: تشغيل SQL Scripts بالترتيب

قم بتشغيل كل ملف بالترتيب التالي (انسخ المحتوى والصقه في SQL Editor ثم اضغط Run):

#### 1. إنشاء جدول الملفات الشخصية
```sql
-- انسخ محتوى scripts/001_create_profiles.sql
```

#### 2. إنشاء جدول الخدمات
```sql
-- انسخ محتوى scripts/002_create_services.sql
```

#### 3. إنشاء جدول العمال
```sql
-- انسخ محتوى scripts/003_create_workers.sql
```

#### 4. إنشاء جدول معرض الأعمال
```sql
-- انسخ محتوى scripts/004_create_worker_portfolio.sql
```

#### 5. إنشاء جدول الطلبات
```sql
-- انسخ محتوى scripts/005_create_service_requests.sql
```

#### 6. إنشاء جدول العطاءات
```sql
-- انسخ محتوى scripts/006_create_bids.sql
```

#### 7. إنشاء جدول التقييمات
```sql
-- انسخ محتوى scripts/007_create_reviews.sql
```

#### 8. إنشاء جدول المحادثات
```sql
-- انسخ محتوى scripts/008_create_chats.sql
```

#### 9. إنشاء جدول الرسائل
```sql
-- انسخ محتوى scripts/009_create_messages.sql
```

#### 10. إنشاء جدول نقاط الولاء
```sql
-- انسخ محتوى scripts/010_create_loyalty_points.sql
```

#### 11. إنشاء View للتوافق (اختياري)
```sql
-- انسخ محتوى scripts/011_create_worker_profiles_view.sql
```

### الخطوة 3: التحقق من الجداول

بعد تشغيل جميع الـ scripts:
1. اذهب إلى **Table Editor** في Supabase Dashboard
2. تأكد من وجود الجداول التالية:
   - ✅ profiles
   - ✅ services
   - ✅ workers
   - ✅ worker_portfolio
   - ✅ service_requests
   - ✅ bids
   - ✅ reviews
   - ✅ chats
   - ✅ messages
   - ✅ loyalty_points

### الخطوة 4: التحقق من البيانات الأولية

تأكد من أن جدول `services` يحتوي على الخدمات الأساسية:
- سباكة
- كهرباء
- نقاشة
- محارة
- تشطيب
- صيانة عامة
- تكييف
- نجارة

إذا لم تكن موجودة، قم بتشغيل `scripts/002_create_services.sql` مرة أخرى.

## ✅ بعد إكمال الإعداد

بعد إكمال جميع الخطوات:
1. تأكد من أن ملف `.env.local` موجود ويحتوي على معلومات Supabase الصحيحة
2. شغل المشروع: `npm run dev`
3. افتح المتصفح على: http://localhost:3000
4. جرب التسجيل وإنشاء حساب جديد

## 🐛 حل المشاكل

### خطأ: "relation does not exist"
- تأكد من تشغيل جميع SQL scripts بالترتيب
- تحقق من أن الجداول موجودة في Table Editor

### خطأ: "permission denied"
- تأكد من استخدام Anon Key الصحيح
- تحقق من RLS Policies في Supabase

### خطأ: "duplicate key"
- بعض الجداول قد تكون موجودة بالفعل
- يمكنك تجاهل هذا الخطأ أو حذف الجدول وإعادة إنشائه

